import { Gamja_Flower, Grandstander, Kode_Mono } from "next/font/google";

export const gamjaFlower = Gamja_Flower({
	subsets: ["latin"],
	weight: "400",
	variable: "--gamja-flower",
});

export const kodeMono = Kode_Mono({
	subsets: ["latin"],
	variable: "--kode-mono",
});

export const grandstander = Grandstander({
	subsets: ["latin"],
	variable: "--grandstander",
});
